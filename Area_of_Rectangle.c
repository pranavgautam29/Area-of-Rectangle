#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter the length of the rectangle:"); 
    scanf("%d", &a); //here we are asking the user the length of the rectangle
    printf("Enter the width of the rectangle:");
    scanf("%d", &b); // here we are asking the user to input the width of the recatngle
    printf("The Area of the Rectangle whose length and width are %d, %d is %d ", a,b , a*b  );

    return 0;
}










































